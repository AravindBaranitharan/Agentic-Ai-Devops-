# RENAME to .env (cp env.sample .env), then paste your key. NEVER commit .env.
OPENAI_API_KEY=sk-paste-your-key-here
MODEL=gpt-4o

# auto  → use a REAL cluster whenever one is reachable (kubeconfig / KUBECONFIG),
#         else fall back to the offline mock. true = same. false = force mock.
USE_REAL_K8S=auto

# Optional: point at a specific kubeconfig for the live path.
# KUBECONFIG=/Users/you/.kube/config
