# AWS ChatOps Agent — dependencies
streamlit==1.60.0      # the chat UI (pin: 1.61 + starlette 1.4 broke gzip → 500s)
starlette<1.4          # keep gzip middleware compatible with streamlit 1.60
openai>=1.30           # the gpt-4o agent brain (tool calling)
boto3>=1.34            # real AWS calls (only used when USE_REAL_AWS=true)
python-dotenv>=1.0     # loads OPENAI_API_KEY from .env

# S3 Freshness page — built with LangChain (1.x create_agent API)
langchain>=1.0         # create_agent tool-calling agent
langchain-openai>=0.2  # OpenAI (gpt-4o) provider for LangChain
